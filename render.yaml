services:
  - type: web
    name: poolvault
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: poolvault-db
          property: connectionString

databases:
  - name: poolvault-db
    databaseName: poolvault
    user: poolvault
